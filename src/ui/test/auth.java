package src.ui.test;

import javax.swing.*;

public class auth extends JFrame {
    int width;
    int height;

    auth(int width, int height) {
        // setLayout(new ());
        setVisible(true);
    }

}
